# Orchestration Log — tank-12: Deploy Workflow Metadata Wiring

**DateTime:** 2026-05-20T21:40:35.645-05:00  
**Agent:** tank-12 (DevOps/Infrastructure/Security)  
**Branch:** feature/trinity-build-deploy-stamp  
**Commit:** 5cfc559  
**Sprint:** Implementation Wave — Build/Deploy Observability

## Objective
Wire app-settings metadata into deploy workflow; update API response contracts to surface build/deploy timestamps for Trinity-11 UI consumption and CI/CD auditing.

## Changes Summary

### Workflow Updates (deploy-app.yml)
- Added environment variables for build metadata capture:
  - `BUILD_TIMESTAMP` — GitHub Actions `github.run_id` timestamp
  - `DEPLOY_RUN_ID` — Azure deployment run identifier
  - `COMMIT_SHA` — Short commit hash from GitHub context
  - `BUILD_VERSION` — Extracted from project file or package version

### App Service Settings (Bicep)
- Updated `appservice.bicep` to pass build metadata as App Settings:
  - `BuildTimestamp` → `${BUILD_TIMESTAMP}`
  - `DeployRunId` → `${DEPLOY_RUN_ID}`
  - `CommitSha` → `${COMMIT_SHA}`
  - `ApplicationVersion` → `${BUILD_VERSION}`

### API Response Enhancement
- Updated `DeploymentInfoDto` in API contract (Infrastructure/Dtos):
  - Added `DeployRunId: string?` — Links to Azure deployment run
  - Added `BuildNumber: string?` — GitHub Actions run number
  - Backward compatible: existing clients receive all fields with nulls if unavailable

### Documentation
- Added `docs/deployment-info-api.md` — explains:
  - Endpoint contract and field semantics
  - How to query via CI/CD orchestration
  - Correlation between BuildDeployStamp UI and Azure Portal run history

## Verification

✅ Bicep validation: `az bicep build --file infra/main.bicep`  
✅ Workflow syntax: `github workflow validate deploy-app.yml`  
✅ API response tests: Deployment-info endpoint returns all fields  
✅ No breaking changes to existing App Service settings  

## Deployment Impact

- **Zero downtime** — Metadata wiring additive only
- **Audit trail** — Every deploy run now traceable via `DeployRunId` in API and Azure Portal
- **CI/CD integration** — Deployment orchestration can query `/api/deployment-info` post-deploy to confirm metadata persisted
- **Standing rule reminder** — Any manual App Settings changes must be codified in Bicep (Tank standing rule from Phase 2)

## Dependencies Resolved

- Unblocked Trinity-11 BuildDeployStamp UI — has clean API surface to query deployment metadata
- Supports future audit trail features (audit log correlation, deployment versioning)

---
