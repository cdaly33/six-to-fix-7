@page "/clients"
@attribute [Authorize(Roles = "TenantAdmin,SuperAdmin")]
@inject NavigationManager Navigation

<PageTitle>Client Management — Six to Fix</PageTitle>

<h1>Client Management</h1>

@*
  TODO (Phase 4): Implement client management.
  Requires IClientService (not yet defined) with methods such as:
    - GetClientsAsync(Guid tenantId) → IReadOnlyList<Client>
    - CreateClientAsync(string name, string slug, Guid tenantId) → Client
    - UpdateClientAsync(Guid clientId, ...) → Client
  Client entity exists in SixToFix.Domain.Entities.
  Also needed: a form component for create/edit with slug validation.
  Register IClientService in DI before wiring this page.
*@

<p class="text-muted">Client management coming in Phase 4.</p>

@code {
    [CascadingParameter] private Task<AuthenticationState> AuthState { get; set; } = null!;

    protected override async Task OnInitializedAsync()
    {
        var auth = await AuthState;
        if (!auth.User.Identity?.IsAuthenticated ?? true)
        {
            Navigation.NavigateTo("/login", forceLoad: false);
            return;
        }
        if (!auth.User.IsInRole("TenantAdmin") && !auth.User.IsInRole("SuperAdmin"))
            Navigation.NavigateTo("/audits", forceLoad: false);
    }
}